import styled from 'styled-components';
import { CloseCircle } from '@styled-icons/remix-fill';

export const CloseButton = styled.div``;

export const CloseButtonIcon = styled(CloseCircle)`
  height: 4.5rem;
  width: 4.5rem;

  cursor: pointer;
`;
