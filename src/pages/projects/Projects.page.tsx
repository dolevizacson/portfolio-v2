import * as React from 'react';

import ProjectsList from '../../components/projects-list/ProjectsList.component';

const Projects = (): JSX.Element => {
  return <ProjectsList />;
};

export default Projects;
